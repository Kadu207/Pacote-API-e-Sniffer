# Contribuindo

Manter o pacote alinhado a stacks modernas e ao Cursor (Skills, Agent).

1. Atualize `scripts/scan-apis.py` e `reference.md`
2. Ajuste `.cursor/skills/sniff-system-apis/SKILL.md`
3. Teste: `python scripts/scan-apis.py --root "..\outro-projeto" --out docs\example-run`
4. Commit e push no GitHub

Tags: `v1.0.0`, `v1.1.0`, etc.
